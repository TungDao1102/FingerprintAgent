---
wave: "03-02"
depends_on: ["03-01"]
files_modified:
  - "src/FingerprintAgent/Configuration/ConfigFileWatcher.cs"
  - "src/FingerprintAgent/Service/FingerprintAgentService.cs"
  - "src/FingerprintAgent/Api/HttpServer.cs"
  - "src/FingerprintAgent/Api/CorsMiddleware.cs"
autonomous: false
---

# 03-02 PLAN: Config Reload (CFG-03)

## Goal

Watch `config.json` for changes using `FileSystemWatcher`, reload only `ScannerConfig` and `CorsConfig` at runtime (no service restart), with debounce to handle double-save patterns from VS/Notepad++.

## Scope

CFG-03 — Runtime config reload.
D-06, D-07, D-08, D-09.

---

## Tasks

```xml
<task id="03-02-1">
  <title>ConfigFileWatcher: Create new class with FileSystemWatcher + debounce timer + disposal</title>
  <file>src/FingerprintAgent/Configuration/ConfigFileWatcher.cs</file>

  <read_first>
    src/FingerprintAgent/Configuration/ConfigLoader.cs
    .planning/phases/03-resilience-runtime-reconfiguration/03-RESEARCH.md (FileSystemWatcher section)
  </read_first>

  <acceptance_criteria>
    - Add new class ConfigFileWatcher with FileSystemWatcher and correct NotifyFilter (LastWrite | Size)
    - Add 300ms debounce Timer (System.Timers.Timer, AutoReset=false) to coalesce double-save
    - On debounce elapsed: loads config via ConfigLoader.LoadFromDirectory() with FileShare.ReadWrite
    - Validates newConfig.Scanner != null and newConfig.Cors != null before firing ConfigReloaded event
    - On bad parse / missing sections: keeps old config, logs error (D-08), does NOT throw
    - ConfigReloaded event passes new AgentConfig
    - Implements IDisposable (disposes watcher and timer)
    - Dispose() method: stops debounce timer first, disposes it, then disposes watcher (order matters)
    - Disposal is idempotent (_disposed flag)
  </acceptance_criteria>

  <description>
    Create new file: src/FingerprintAgent/Configuration/ConfigFileWatcher.cs

    using FingerprintAgent.Logging;

    public class ConfigFileWatcher : IDisposable
    {
        private readonly FileSystemWatcher _watcher;
        private readonly Timer _debounceTimer;
        private readonly AgentLogger _logger;
        private readonly string _configPath;
        private bool _disposed;

        public event Action<AgentConfig> ConfigReloaded;

        public ConfigFileWatcher(string configPath, AgentLogger logger)
        {
            _configPath = configPath;
            _logger = logger;

            var directory = Path.GetDirectoryName(configPath);
            var fileName = Path.GetFileName(configPath);

            _watcher = new FileSystemWatcher(directory, fileName)
            {
                NotifyFilter = NotifyFilters.LastWrite | NotifyFilters.Size,
                EnableRaisingEvents = true
            };
            _watcher.Changed += OnRawChanged;

            _debounceTimer = new Timer(300);
            _debounceTimer.AutoReset = false;
            _debounceTimer.Elapsed += OnDebounceElapsed;
        }

        private void OnRawChanged(object sender, FileSystemEventArgs e)
        {
            _debounceTimer.Stop();
            _debounceTimer.Start();
        }

        private void OnDebounceElapsed(object sender, ElapsedEventArgs e)
        {
            try
            {
                _logger?.Info(null, "ConfigFileWatcher: config changed, reloading");

                // Read with FileShare.ReadWrite to avoid locking issues while file is open
                string json;
                using (var fs = new FileStream(_configPath, FileMode.Open, FileAccess.Read, FileShare.ReadWrite))
                using (var reader = new StreamReader(fs))
                {
                    json = reader.ReadToEnd();
                }

                // Parse via ConfigLoader
                var directory = Path.GetDirectoryName(_configPath);
                var newConfig = ConfigLoader.LoadFromDirectory(directory);

                // Validate D-06: only ScannerConfig and CorsConfig are reloaded
                if (newConfig.Scanner == null || newConfig.Cors == null)
                {
                    _logger?.Error(null, "ConfigFileWatcher: reload missing ScannerConfig or CorsConfig — keeping old config");
                    return;
                }

                ConfigReloaded?.Invoke(newConfig);
                _logger?.Info(null, "ConfigFileWatcher: config reload complete");
            }
            catch (Exception ex)
            {
                // D-08: keep old config, log error, don't crash
                _logger?.Error(null, $"ConfigFileWatcher: reload failed, keeping old config — {ex.Message}");
            }
        }

        public void Dispose()
        {
            if (_disposed) return;
            _disposed = true;
            // Order matters: stop timer first, then watcher
            try { _debounceTimer?.Stop(); } catch { }
            try { _debounceTimer?.Dispose(); } catch { }
            if (_watcher != null)
            {
                _watcher.EnableRaisingEvents = false;
                _watcher.Changed -= OnRawChanged;
                try { _watcher.Dispose(); } catch { }
            }
        }
    }

    Note: Use System.IO.FileStream for reading with FileShare.ReadWrite.
    Note: Use System.Timers.Timer for the debounce (300ms, AutoReset=false).
    Note: Dispose() order: stop/dispose timer first, then dispose watcher.
  </description>
</task>

<task id="03-02-2">
  <title>CorsMiddleware: Add UpdateConfig method for hot-reload</title>
  <file>src/FingerprintAgent/Api/CorsMiddleware.cs</file>

  <read_first>
    src/FingerprintAgent/Api/CorsMiddleware.cs
    src/FingerprintAgent/Configuration/AgentConfig.cs
  </read_first>

  <acceptance_criteria>
    - Remove readonly from _mode and _allowedOrigins fields (change to private with underscore convention)
    - Add private readonly object _corsLock = new object()
    - UpdateConfig(mode, allowedOrigins) method updates _mode and _allowedOrigins fields under lock
    - _allowedOrigins is replaced with a new HashSet (not modified in place)
    - Accepts null mode — treats as "wildcard"
    - Accepts null allowedOrigins — treats as empty array
    - ApplyCorsHeaders and HandleCorsPreflight use lock(_corsLock) when reading _mode AND _allowedOrigins together
  </acceptance_criteria>

  <description>
    Changes to CorsMiddleware fields:

    BEFORE:
    private readonly string _mode;
    private readonly HashSet<string> _allowedOrigins;

    AFTER:
    private string _mode;
    private HashSet<string> _allowedOrigins;
    private readonly object _corsLock = new object();

    Add UpdateConfig method:
    public void UpdateConfig(string mode, string[] allowedOrigins)
    {
        lock (_corsLock)
        {
            _mode = mode ?? "wildcard";
            _allowedOrigins = new HashSet<string>(
                allowedOrigins ?? Array.Empty<string>(),
                StringComparer.OrdinalIgnoreCase);
        }
    }

    Update ApplyCorsHeaders to read under lock:
    public void ApplyCorsHeaders(HttpListenerResponse response, string origin)
    {
        if (string.IsNullOrEmpty(origin))
            return;

        string mode;
        HashSet<string> allowedOrigins;
        lock (_corsLock) { mode = _mode; allowedOrigins = _allowedOrigins; }

        if (mode == "wildcard")
        {
            response.Headers.Add("Access-Control-Allow-Origin", "*");
        }
        else if (mode == "allowlist" && allowedOrigins.Contains(origin))
        {
            response.Headers.Add("Access-Control-Allow-Origin", origin);
            response.Headers.Add("Vary", "Origin");
        }
        // ... rest unchanged
    }

    Update HandleCorsPreflight similarly — read _mode and _allowedOrigins under lock before using.
  </description>
</task>

<task id="03-02-3">
  <title>HttpServer: Add UpdateCorsConfig public method</title>
  <file>src/FingerprintAgent/Api/HttpServer.cs</file>

  <read_first>
    src/FingerprintAgent/Api/HttpServer.cs
    src/FingerprintAgent/Api/CorsMiddleware.cs (after task 2)
  </read_first>

  <acceptance_criteria>
    - Add public void UpdateCorsConfig(CorsConfig newCors) method to HttpServer
    - Calls _cors.UpdateConfig(newCors.Mode, newCors.AllowedOrigins)
    - Thread-safe: no concurrent modification of _listener or other server state
  </acceptance_criteria>

  <description>
    Add to HttpServer class:

    public void UpdateCorsConfig(CorsConfig newCors)
    {
        _cors.UpdateConfig(newCors.Mode, newCors.AllowedOrigins);
    }

    This is the integration point between ConfigFileWatcher and CorsMiddleware.
    Called by FingerprintAgentService after a successful config reload.
  </description>
</task>

<task id="03-02-4">
  <title>FingerprintAgentService: Wire up ConfigFileWatcher in OnStart/OnStop with disposal</title>
  <file>src/FingerprintAgent/Service/FingerprintAgentService.cs</file>

  <read_first>
    src/FingerprintAgent/Service/FingerprintAgentService.cs
    src/FingerprintAgent/Configuration/ConfigFileWatcher.cs (after task 1)
    src/FingerprintAgent/Api/HttpServer.cs (after task 3)
  </read_first>

  <acceptance_criteria>
    - Add _configWatcher field of type ConfigFileWatcher
    - Add _configLock field (readonly object) for thread-safe config access
    - ConfigFileWatcher created in OnStart() with path to config.json (after _httpServer.Start())
    - Subscribes to ConfigReloaded event
    - On reload: updates _config (thread-safe), updates HttpServer via UpdateCorsConfig(), logs reload
    - Active adapter is NOT reset on reload (D-09)
    - ConfigFileWatcher disposed in OnStop() BEFORE scanner disposal (in its own try-catch)
    - Disposal is also wrapped in try-catch so failure doesn't crash the shutdown sequence
    - If OnStart throws after _configWatcher is created, dispose it in the catch block
  </acceptance_criteria>

  <description>
    Add field to FingerprintAgentService:
    private ConfigFileWatcher _configWatcher;

    In OnStart(), after _httpServer.Start() and StartHealthCheckTimer():

    var configPath = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "config.json");
    _configWatcher = new ConfigFileWatcher(configPath, _logger);
    _configWatcher.ConfigReloaded += OnConfigReloaded;

    Add private handler:

    private void OnConfigReloaded(AgentConfig newConfig)
    {
        var cid = AgentLogger.GenerateCorrelationId();

        // D-06: Only reload ScannerConfig + CorsConfig
        // D-09: Keep active adapter — do NOT recreate ScannerManager
        lock (_configLock)
        {
            _config = newConfig;
        }

        // Update CORS immediately
        _httpServer?.UpdateCorsConfig(newConfig.Cors);

        _logger?.Info(cid, $"ConfigFileWatcher: applied scanner priority=[{string.Join(", ", newConfig.Scanner.Priority)}], cors mode={newConfig.Cors.Mode}");
    }

    Add _configLock field:
    private readonly object _configLock = new object();

    In OnStop(), add ConfigFileWatcher disposal BEFORE scanner disposal, in its own try-catch:
    ```
    try { _configWatcher?.Dispose(); }
    catch { /* best-effort, may already be disposed */ }
    ```

    NOTE: ConfigFileWatcher monitors config.json at the application's base directory.
    ConfigLoader.Load() uses AppDomain.CurrentDomain.BaseDirectory — consistent path.
  </description>
</task>

<task id="03-02-5">
  <title>ScannerManager: Add UpdatePriority method for runtime priority reload</title>
  <file>src/FingerprintAgent/Adapters/ScannerManager.cs</file>

  <read_first>
    src/FingerprintAgent/Adapters/ScannerManager.cs
  </read_first>

  <acceptance_criteria>
    - Add public void UpdatePriority(string[] newPriority) to ScannerManager
    - Re-creates adapter list from newPriority via CreateAdapter()
    - Active adapter reference is preserved (D-09) — existing _activeAdapter stays set
    - Unknown vendor names throw InvalidOperationException (fail-fast, consistent with constructor)
    - Backoff state is preserved (not reset) on priority change
    - Thread-safe: uses _adapterLock
  </acceptance_criteria>

  <description>
    Add to ScannerManager:

    public void UpdatePriority(string[] newPriority)
    {
        if (newPriority == null || newPriority.Length == 0)
            return;

        lock (_adapterLock)
        {
            var vendorList = new List<IScannerAdapter>();
            foreach (var vendorName in newPriority)
            {
                IScannerAdapter adapter = CreateAdapter(vendorName);
                vendorList.Add(adapter);
            }
            _adapters = vendorList.ToArray();
        }

        _logger?.Info(null, $"ScannerManager: priority updated, new order=[{string.Join(", ", newPriority)}]");
    }

    Note: Active adapter (_activeAdapter) is NOT touched — stays as-is (D-09).
    Note: Backoff state is in separate fields and not affected.
    Note: The new adapter list is used on the NEXT Scan() call.

    Call this from FingerprintAgentService.OnConfigReloaded after updating _config:
    ((ScannerManager)_scanner).UpdatePriority(newConfig.Scanner.Priority);

    This requires casting IScannerAdapter to ScannerManager — acceptable here since we control the concrete type.
  </description>
</task>

<task id="03-02-6">
  <title>FingerprintAgentService: Call UpdatePriority in OnConfigReloaded handler</title>
  <file>src/FingerprintAgent/Service/FingerprintAgentService.cs</file>

  <read_first>
    src/FingerprintAgent/Service/FingerprintAgentService.cs (after task 4)
  </read_first>

  <acceptance_criteria>
    - OnConfigReloaded calls ScannerManager.UpdatePriority() with newConfig.Scanner.Priority (added in task 03-02-5)
    - If _scanner is not ScannerManager (e.g., MockScannerAdapter), skip gracefully (no crash)
  </acceptance_criteria>

  <description>
    Update OnConfigReloaded to also update scanner priority:

    private void OnConfigReloaded(AgentConfig newConfig)
    {
        var cid = AgentLogger.GenerateCorrelationId();

        lock (_configLock)
        {
            _config = newConfig;
        }

        // D-06: only reload Scanner + Cors
        _httpServer?.UpdateCorsConfig(newConfig.Cors);

        // D-09: active adapter stays the same, but new priority applies on next failure
        var scannerManager = _scanner as ScannerManager;
        scannerManager?.UpdatePriority(newConfig.Scanner.Priority);

        _logger?.Info(cid, $"ConfigFileWatcher: applied scanner priority=[{string.Join(", ", newConfig.Scanner.Priority)}], cors mode={newConfig.Cors.Mode}");
    }

    The `as ScannerManager` cast is safe — in production, _scanner is always ScannerManager.
    MockScannerAdapter doesn't support UpdatePriority but it doesn't need to (MockMode handles it).
  </description>
</task>
```

---

## Verification

- `dotnet build src/FingerprintAgent/FingerprintAgent.csproj` → 0 errors, 0 warnings
- `dotnet test src/FingerprintAgent.Tests/` → all existing tests pass
- FileSystemWatcher starts watching on service start
- Config file save triggers reload within ~500ms (debounce + load time)
- On bad JSON: old config stays active, error logged, no crash
- CORS headers reflect updated config after reload
- Active adapter unchanged after reload (same DeviceId before/after)
- Timer + FileSystemWatcher both disposed on service stop (explicit OnStop disposal verified)

---

## must_haves

1. **ConfigFileWatcher class**: FileSystemWatcher + 300ms debounce timer, disposed on service stop
2. **Reload event**: fires `Action<AgentConfig>` after successful parse + validation
3. **Bad config**: caught exception keeps old config, logs error, no crash (D-08)
4. **CORS hot-reload**: `HttpServer.UpdateCorsConfig()` → `CorsMiddleware.UpdateConfig()` → new origins immediately active
5. **Scanner priority hot-reload**: `ScannerManager.UpdatePriority()` creates new adapter list, preserves active adapter (D-09)
6. **Only Scanner + Cors**: D-06 — HTTP/service config NOT reloadable without restart
7. **Thread-safe**: config reference replaced under lock, reads copy to local var
8. **ConfigFileWatcher OnStop disposal**: `configWatcher?.Dispose()` called in its own try-catch in OnStop, before scanner disposal
9. **Build clean**: 0 warnings, 0 errors
10. **All existing tests pass**: no regression