---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/FingerprintAgent/Adapters/ScannerManager.cs
  - src/FingerprintAgent/Service/FingerprintAgentService.cs
  - src/FingerprintAgent/Adapters/MockScannerAdapter.cs
  - config.json
autonomous: true
requirements:
  - SCAN-04
  - SCAN-05
must_haves:
  truths:
    - ScannerManager selects adapters in priority order until one succeeds per capture (D-04)
    - ScannerManager enforces 10-second total timeout across all adapter attempts (D-06)
    - FingerprintAgentService creates ScannerManager when MockMode=false
    - HealthHandler exposes DeviceId and Model from the active adapter
  artifacts:
    - src/FingerprintAgent/Adapters/ScannerManager.cs
    - SCANNER_SETUP.md
  key_links:
    - ScannerManager.Scan() is called by CaptureHandler — same contract as any IScannerAdapter
    - ScannerManager delegates IsConnected/DeviceId/Model to active adapter for HealthHandler
---

<objective>
Implement ScannerManager (composite IScannerAdapter, priority-based fallback, 10s total timeout) and wire it into FingerprintAgentService — SCAN-04, SCAN-05.
</objective>

<execution_context>
@C:/Users/admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@src/FingerprintAgent/Service/FingerprintAgentService.cs
@src/FingerprintAgent/Api/CaptureHandler.cs
@src/FingerprintAgent/Api/HealthHandler.cs
@src/FingerprintAgent/Adapters/IScannerAdapter.cs
@src/FingerprintAgent/Adapters/MockScannerAdapter.cs
@src/FingerprintAgent/Configuration/AgentConfig.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ScannerManager (IScannerAdapter composite with priority fallback)</name>
  <files>src/FingerprintAgent/Adapters/ScannerManager.cs</files>
  <read_first>
    src/FingerprintAgent/Adapters/IScannerAdapter.cs
    src/FingerprintAgent/Adapters/CaptureResult.cs
    src/FingerprintAgent/Configuration/AgentConfig.cs
    src/FingerprintAgent/Adapters/SecuGenAdapter.cs
    src/FingerprintAgent/Adapters/DigitalPersonaAdapter.cs
    src/FingerprintAgent/Adapters/FutronicAdapter.cs
  </read_first>
  <behavior>
    - ScannerManager holds a list of IScannerAdapter in priority order from config.Scanner.Priority
    - Scan() iterates adapters: initialize → scan → return on success; on failure try next
    - 10-second CancellationTokenSource.CancelAfter covers all adapter attempts combined
    - Per-adapter timeout: ~3 seconds (connect + capture) via linked CancellationTokenSource
    - On timeout: return CaptureResult with ErrorMessage="CAPTURE_TIMEOUT"
    - On all fail: return CaptureResult with ErrorMessage="SCANNER_NOT_CONNECTED"
    - IsConnected/DeviceId/Model delegate to _activeAdapter after successful scan
    - MockMode=true bypasses all real adapters and uses MockScannerAdapter directly
  </behavior>
  <action>
    Create src/FingerprintAgent/Adapters/ScannerManager.cs implementing IScannerAdapter:

    1. Class fields: IScannerAdapter[] _adapters (in priority order); IScannerAdapter _activeAdapter; AgentLogger _logger; ScannerConfig _config; CancellationTokenSource _cts; string _lastVendorErrorCode; bool _mockMode.

    2. Constructor: ScannerManager(AgentConfig config, AgentLogger logger). If config.Scanner.MockMode == true: _mockAdapter = new MockScannerAdapter(); _mockMode = true; return. Else: build _adapters list from config.Scanner.Priority array by instantiating per vendor name: "SecuGen" → new SecuGenAdapter(), "DigitalPersona" → new DigitalPersonaAdapter(), "Futronic" → new FutronicAdapter(). Log each adapter instantiation at INFO level. If vendor name not recognized, log WARNING and skip.

    3. IScannerAdapter properties: IsConnected (get { return _mockMode ? _mockAdapter.IsConnected : (_activeAdapter?.IsConnected ?? false); }), DeviceId (get { return _mockMode ? _mockAdapter.DeviceId : (_activeAdapter?.DeviceId ?? "no-device"); }), Model (get { return _mockMode ? _mockAdapter.Model : (_activeAdapter?.Model ?? "no-device"); }), MimeType ("image/png"), VendorErrorCode (get { return _mockMode ? "MOCK" : (_activeAdapter?.VendorErrorCode ?? "NO_ADAPTER"); }).

    4. Scan() method: if (_mockMode) return _mockAdapter.Scan(). Create linked CancellationTokenSource: var totalCts = CancellationTokenSource.CreateLinkedTokenSource(_cts.Token); totalCts.CancelAfter(TimeSpan.FromSeconds(10)); // D-06 total budget. foreach (var adapter in _adapters) { if (totalCts.Token.IsCancellationRequested) { _logger?.Warning("ScannerManager: total timeout exceeded"); return new CaptureResult { IsSuccess = false, ErrorMessage = "CAPTURE_TIMEOUT" }; } var adapterCts = CancellationTokenSource.CreateLinkedTokenSource(totalCts.Token); adapterCts.CancelAfter(TimeSpan.FromSeconds(3)); // D-06 per-adapter budget. try { _logger?.Debug($"Trying adapter {adapter.GetType().Name}"); if (adapter.Initialize()) { var result = adapter.Scan(); if (result.IsSuccess) { _activeAdapter = adapter; _logger?.Info($"ScannerManager: {adapter.GetType().Name} succeeded, DeviceId={adapter.DeviceId}"); return result; } else { _logger?.Warning($"ScannerManager: {adapter.GetType().Name} scan failed: {result.ErrorMessage}"); } } else { _logger?.Warning($"ScannerManager: {adapter.GetType().Name} initialize returned false: {adapter.VendorErrorCode}"); } } catch (Exception ex) { _logger?.Error($"ScannerManager: {adapter.GetType().Name} threw {ex.GetType().Name}: {ex.Message}"); } } _logger?.Error("ScannerManager: all adapters failed"); return new CaptureResult { IsSuccess = false, ErrorMessage = "SCANNER_NOT_CONNECTED" }.

    5. If _activeAdapter is set after a successful scan, all property getters delegate to it for HealthHandler.
  </action>
  <verify>
    <automated>dotnet build src/FingerprintAgent/FingerprintAgent.csproj -c Release 2>&1 | findstr /i error</automated>
  </verify>
  <done>ScannerManager implements IScannerAdapter, selects adapters in priority order, enforces 10s total and ~3s per-adapter timeouts, delegates to active adapter after successful scan.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ScannerManager into FingerprintAgentService (MockMode support)</name>
  <files>
    src/FingerprintAgent/Service/FingerprintAgentService.cs
    src/FingerprintAgent/Adapters/MockScannerAdapter.cs
  </files>
  <read_first>
    src/FingerprintAgent/Service/FingerprintAgentService.cs
    src/FingerprintAgent/Configuration/AgentConfig.cs
  </read_first>
  <action>
    Modify FingerprintAgentService.OnStart to replace MockScannerAdapter with ScannerManager:

    1. Read the current _scanner assignment: `_scanner = new MockScannerAdapter();`

    2. Replace with: `_scanner = new ScannerManager(_config, _logger);`

    3. ScannerManager handles MockMode internally — when config.Scanner.MockMode == true it wraps MockScannerAdapter transparently. No other code changes needed in FingerprintAgentService, CaptureHandler, or HealthHandler.

    4. Update MockScannerAdapter to implement the extended IScannerAdapter (Initialize() and VendorErrorCode) with trivial stubs: Initialize() returns true; VendorErrorCode returns "MOCK".

    5. Verify the csproj already has PlatformTarget x86 from Plan 02-01 Task 3.
  </action>
  <verify>
    <automated>dotnet build src/FingerprintAgent/FingerprintAgent.csproj -c Release 2>&1 | findstr /i error</automated>
  </verify>
  <done>FingerprintAgentService creates ScannerManager(_config, _logger) instead of MockScannerAdapter. MockScannerAdapter has Initialize() and VendorErrorCode stubs. CaptureHandler and HealthHandler work unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add ScannerManager unit tests</name>
  <files>src/FingerprintAgent.Tests/ScannerManagerTests.cs</files>
  <read_first>
    src/FingerprintAgent/Adapters/ScannerManager.cs
    src/FingerprintAgent/Adapters/IScannerAdapter.cs
  </read_first>
  <action>
    Create src/FingerprintAgent.Tests/ScannerManagerTests.cs with xUnit tests using Moq:

    1. ScannerManager_TriesAdaptersInPriorityOrder: mock adapter1.Initialize()=false, mock adapter2.Initialize()=true, adapter2.Scan()=success. Create ScannerManager(new[] {adapter1, adapter2}). Call Scan(). Verify adapter1.Initialize() called once, adapter1.Scan() NEVER called, adapter2.Initialize() called once, adapter2.Scan() called once. Assert result.IsSuccess==true.

    2. ScannerManager_ReturnsNotConnected_WhenAllAdaptersFail: both adapters Initialize()=false. ScannerManager with both. Call Scan(). Assert result.IsSuccess==false and result.ErrorMessage=="SCANNER_NOT_CONNECTED".

    3. ScannerManager_ExposesActiveAdapterProperties: mock adapter with DeviceId="secugen-001", Model="Hamster Pro 20", IsConnected=true. ScannerManager.Scan() succeeds. Assert ScannerManager.DeviceId=="secugen-001" and ScannerManager.Model=="Hamster Pro 20" and ScannerManager.IsConnected==true.

    4. ScannerManager_RespectsMockMode: create AgentConfig with Scanner.MockMode=true, pass to ScannerManager. Call Scan(). Verify it returns success with mock scanner PNG bytes (not calling any real adapter). Assert ScannerManager.VendorErrorCode=="MOCK".

    5. ScannerManager_Timeout_ReturnsTimeout: mock adapter Initialize()=true but Scan() never returns (or uses a long timeout). ScannerManager with 10s total budget — test that the linked CTS eventually times out and returns "CAPTURE_TIMEOUT". Use a mock that spins forever on Scan() and verify the outer timeout fires.

    Use Moq.Quick to create mocks: var mock = new Mock<IScannerAdapter>();
  </action>
  <verify>
    <automated>dotnet test src/FingerprintAgent.Tests/FingerprintAgent.Tests.csproj --filter "FullyQualifiedName~ScannerManager" 2>&1 | findstr /i "Passed Failed"</automated>
  </verify>
  <done>ScannerManagerTests.cs has 5 tests covering priority order, all-fail, active adapter delegation, mock mode, and timeout.</done>
</task>

<task type="auto">
  <name>Task 4: Create SCANNER_SETUP.md setup documentation</name>
  <files>SCANNER_SETUP.md</files>
  <read_first>
    .planning/phases/02-multi-vendor-scanner-adapters/02-RESEARCH.md (DLL locations, download links)
  </read_first>
  <action>
    Create SCANNER_SETUP.md at project root:

    1. Document DLL requirements per vendor:
       - SecuGen: copy sgfplib.dll and sgfpamx.dll from SDK Bin\i386\ to lib\SecuGen\ or install directory
       - Digital Persona: copy DPFPDevNET.dll, DPFPEngNET.dll, DPFPGuiNET.dll, DPFPShrNET.dll, DPFPVerNET.dll from U.are.U SDK to lib\DigitalPersona\ or install directory
       - Futronic: copy ftrScanAPI.dll from Futronic Standard SDK to lib\Futronic\ or install directory

    2. Download links (research section sources):
       - SecuGen: https://www.secugen.com/download (free SDK registration required)
       - Digital Persona: https://sdk.hidglobal.com/developer-center/digitalpersona (U.are.U 4500 SDK)
       - Futronic: http://www.futronic-tech.com/download.html (Standard SDK v4.2 registration required)

    3. Assembly reference instructions: for SecuGen.FDxSDKPro.Windows.dll, add HintPath in csproj pointing to lib\SecuGen\. For DPUruNet, use NuGet. For Futronic, P/Invoke loads from DLL name only (no reference needed).

    4. Build requirement: <PlatformTarget>x86</PlatformTarget> — all three SDKs are 32-bit. AnyCPU build will crash with BadImageFormatException on 64-bit Windows.

    5. Distribution: all DLLs go into the same folder as FingerprintAgent.exe per D-08.

    6. License notes: verify commercial distribution rights for each vendor before shipping. SecuGen free SDK may be evaluation-only. Futronic Standard SDK may be watermarked.
  </action>
  <verify>
    <automated>Test-Path SCANNER_SETUP.md</automated>
  </verify>
  <done>SCANNER_SETUP.md documents all three vendors' DLL requirements, download links, and build constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScannerManager → Adapter | Composite delegation — ScannerManager catches exceptions from each adapter |
| Config → Runtime | Priority array from config.json determines adapter order |
| Timeout budget | Nested CancellationTokenSource — outer 10s budget must fire even if inner adapters block |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial | Nested CTS timeout not propagating correctly | medium | mitigate | Test timeout path in ScannerManagerTests — verify outer CTS fires |
| T-02-08 | Information Disclosure | VendorErrorCode exposed via health endpoint | low | accept | Diagnostic-only; no sensitive data |
| T-02-09 | Denial | ScannerManager with empty _adapters list (config error) | low | mitigate | ScannerManager constructor logs ERROR if priority list is empty or all invalid |
</threat_model>

<verification>
- `dotnet build src/FingerprintAgent/FingerprintAgent.csproj -c Release` succeeds with 0 errors
- `dotnet test src/FingerprintAgent.Tests/FingerprintAgent.Tests.csproj --filter "FullyQualifiedName~ScannerManager"` passes
- `SCANNER_SETUP.md` exists at project root
</verification>

<success_criteria>
- ScannerManager.cs implements IScannerAdapter and handles priority fallback with 10s timeout
- FingerprintAgentService uses `new ScannerManager(_config, _logger)` instead of `new MockScannerAdapter()`
- MockScannerAdapter implements the extended IScannerAdapter with Initialize()=true and VendorErrorCode="MOCK"
- ScannerManagerTests.cs has 5 passing unit tests
- SCANNER_SETUP.md exists with DLL copy instructions and download links
</success_criteria>

<output>
Create `.planning/phases/02-multi-vendor-scanner-adapters/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Symbol/Path |
|----------|------|-------------|
| ScannerManager.cs | class | IScannerAdapter composite; _adapters[]; Scan() with priority fallback + timeout; IsConnected/DeviceId/Model delegating to _activeAdapter |
| MockScannerAdapter.cs | class (updated) | Initialize() returns true; VendorErrorCode returns "MOCK" (D-02 compliance) |
| FingerprintAgentService.cs | class (updated) | _scanner = new ScannerManager(_config, _logger) in OnStart |
| ScannerManagerTests.cs | test class | 5 xUnit tests with Moq |
| SCANNER_SETUP.md | doc | DLL copy instructions, download links, license notes, PlatformTarget x86 reminder |